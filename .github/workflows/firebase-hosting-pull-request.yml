# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- ¡PASO AGREGADO: Configurar Node.js! ---
      - name: Setup Node.js environment
        uses: actions/setup-node@v4 # Usa la última versión para Node.js
        with:
          node-version: '20' # Puedes cambiar '20' por la versión de Node.js que uses en tu proyecto (ej. '18', '22')

      # --- ¡PASO AGREGADO: Instalar Dependencias! ---
      - name: Install dependencies
        run: npm install # Esto instala todas las dependencias de package.json

      - run: npm run build # Este paso ahora debería encontrar 'vite'

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAOBRA_478D2 }}
          projectId: laobra-478d2